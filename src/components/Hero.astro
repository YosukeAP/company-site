---
// Hero section with mission statement - AssetPartners version
---

<section class="relative w-full h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-slate-900 via-blue-900 to-purple-900" style="height: 100dvh; max-height: 100dvh;">
  <!-- Abstract Background Pattern -->
  <div class="absolute inset-0 opacity-20">
    <div class="absolute inset-0" style="background-image: radial-gradient(circle at 25% 25%, rgba(59, 130, 246, 0.5) 0%, transparent 50%), radial-gradient(circle at 75% 75%, rgba(147, 51, 234, 0.5) 0%, transparent 50%);"></div>
  </div>

  <!-- Container -->
  <div class="relative z-10 max-w-[1400px] mx-auto px-6 sm:px-8 lg:px-12 xl:px-16 w-full h-full flex items-center py-4 lg:py-6">
    <div class="grid lg:grid-cols-[1.15fr_0.85fr] gap-12 lg:gap-16 xl:gap-20 w-full items-center">
      
      <!-- Left: Mission Content -->
      <div class="text-white">
        <!-- Label -->
        <p class="text-xs sm:text-sm font-bold mb-4 sm:mb-5 lg:mb-6 opacity-90 tracking-[0.2em] uppercase text-blue-300 letter-spacing-wide">
          AI × 専門家 = ハイブリッド成長戦略
        </p>

        <!-- Title -->
        <h1 class="hero-fixed-heading font-extrabold text-white tracking-tight">
          <span>事業資産を最大化する</span>
          <span class="hero-fixed-heading__accent">AIと専門家のハイブリッド</span>
          <span>グロース戦略</span>
        </h1>

        <!-- Description -->
        <p class="hero-fixed-description max-w-3xl">
          <span>BtoBマーケティングの仕組み化と新規事業のグロースを</span>
          <span>AIと専門家チームがワンストップで支援します。</span>
        </p>

        <!-- Dual CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-3 sm:gap-4">
          <button class="bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white font-bold px-6 sm:px-8 py-3 sm:py-4 rounded-xl transition-all duration-300 text-sm sm:text-base shadow-2xl hover:shadow-blue-500/25 hover:scale-[1.02] active:scale-[0.98]">
            無料相談・お問い合わせ
          </button>
          <button class="bg-white/15 hover:bg-white/25 backdrop-blur-md text-white font-bold px-6 sm:px-8 py-3 sm:py-4 rounded-xl transition-all duration-300 text-sm sm:text-base border-2 border-white/40 hover:border-white/60 shadow-lg hover:shadow-xl hover:scale-[1.02] active:scale-[0.98]">
            SaaSデモ・資料請求
          </button>
        </div>
      </div>

      <!-- Right: Visual Elements -->
      <div class="relative h-full min-h-[400px] lg:min-h-0 hidden lg:flex items-center justify-center">
        <!-- Abstract visualization: AI + Strategy -->
        <div class="relative w-full max-w-md">
          <!-- Connected Nodes (representing AI network) -->
          <div class="absolute inset-0 flex items-center justify-center opacity-30">
            <svg viewBox="0 0 400 400" class="w-full">
              <circle cx="200" cy="200" r="150" fill="none" stroke="rgba(59, 130, 246, 0.5)" stroke-width="2"/>
              <circle cx="200" cy="80" r="40" fill="rgba(147, 51, 234, 0.3)"/>
              <circle cx="320" cy="200" r="40" fill="rgba(59, 130, 246, 0.3)"/>
              <circle cx="200" cy="320" r="40" fill="rgba(147, 51, 234, 0.3)"/>
              <circle cx="80" cy="200" r="40" fill="rgba(59, 130, 246, 0.3)"/>
              <line x1="200" y1="80" x2="320" y2="200" stroke="rgba(255, 255, 255, 0.2)" stroke-width="2"/>
              <line x1="320" y1="200" x2="200" y2="320" stroke="rgba(255, 255, 255, 0.2)" stroke-width="2"/>
              <line x1="200" y1="320" x2="80" y2="200" stroke="rgba(255, 255, 255, 0.2)" stroke-width="2"/>
              <line x1="80" y1="200" x2="200" y2="80" stroke="rgba(255, 255, 255, 0.2)" stroke-width="2"/>
            </svg>
          </div>
          <!-- Strategy card visual -->
          <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 bg-white/10 backdrop-blur-lg rounded-xl p-6 lg:p-8 border border-white/20 shadow-2xl w-full max-w-[320px]">
            <div class="text-white">
              <h3 class="text-lg lg:text-xl font-bold mb-3 lg:mb-4">戦略 × 実行</h3>
              <div class="space-y-2 text-xs lg:text-sm opacity-90">
                <div class="flex items-center gap-2">
                  <div class="w-2 h-2 bg-blue-400 rounded-full"></div>
                  <span>AIによる効率化</span>
                </div>
                <div class="flex items-center gap-2">
                  <div class="w-2 h-2 bg-purple-400 rounded-full"></div>
                  <span>専門家の監修・品質担保</span>
                </div>
                <div class="flex items-center gap-2">
                  <div class="w-2 h-2 bg-blue-400 rounded-full"></div>
                  <span>事業成長への伴走</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>
