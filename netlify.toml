# Netlify configuration
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Form handling is automatic with Netlify Forms
# Just ensure the form has netlify attribute

